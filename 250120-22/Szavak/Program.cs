using System;

class Program{
    
    static string[,] szoveg = new string[30, 30];
    static char[] karakter = {' ','!','?',',',':',';', '-', '_', '[', ']', '{', '}', '<', '>', '/', '\\', '|', '@', '#', '$', '%', '^', '&', '*', '('};
    
    static void Main(){
        System.Console.WriteLine("Mondat:");
        string input = Console.ReadLine();
        System.Console.WriteLine();

        int len = input.Length;        
    }


}